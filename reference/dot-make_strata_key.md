# Create strata key string from data frame rows

Create strata key string from data frame rows

## Usage

``` r
.make_strata_key(df, strata_vars)
```

## Arguments

- df:

  Data frame.

- strata_vars:

  Character vector of column names.

## Value

Character vector of strata identifiers.
