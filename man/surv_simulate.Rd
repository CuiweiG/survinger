% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07-simulate.R
\name{surv_simulate}
\alias{surv_simulate}
\title{Simulate genomic surveillance data}
\usage{
surv_simulate(
  n_regions = 5L,
  n_weeks = 26L,
  total_positive_per_week = 1000L,
  sequencing_rates = NULL,
  lineage_dynamics = NULL,
  delay_params = list(mu = 10, size = 3),
  sources = c("clinical", "wastewater", "sentinel"),
  source_weights = c(0.7, 0.2, 0.1),
  seed = NULL
)
}
\arguments{
\item{n_regions}{Integer. Number of geographic regions. Default 5.}

\item{n_weeks}{Integer. Number of epiweeks. Default 26.}

\item{total_positive_per_week}{Integer. Mean total positive cases
per week across all regions. Default 1000.}

\item{sequencing_rates}{Numeric vector of length \code{n_regions}.
Per-region sequencing probability. If \code{NULL}, generated from
a Beta distribution with realistic inequality. Default \code{NULL}.}

\item{lineage_dynamics}{Named list of functions, each taking a
week number and returning a positive weight. If \code{NULL}, uses
a default four-lineage scenario. Default \code{NULL}.}

\item{delay_params}{List with \code{mu} and \code{size} for negative binomial
reporting delay. Default \code{list(mu = 10, size = 3)}.}

\item{sources}{Character vector of sample source types.
Default \code{c("clinical", "wastewater", "sentinel")}.}

\item{source_weights}{Numeric vector (same length as \code{sources}).
Default \code{c(0.7, 0.2, 0.1)}.}

\item{seed}{Integer or \code{NULL}. Random seed. Default \code{NULL}.}
}
\value{
A named list with elements:
\describe{
\item{sequences}{Tibble of individual sequence records.}
\item{population}{Tibble with one row per region.}
\item{truth}{Tibble of true lineage prevalence by region and week.}
\item{parameters}{List of all input parameters.}
}
}
\description{
Generates synthetic surveillance datasets with realistic features:
multiple regions with unequal sequencing rates, multiple lineages
with time-varying prevalence, configurable reporting delays, and
multiple sample sources.
}
\examples{
sim <- surv_simulate(n_regions = 3, n_weeks = 8, seed = 42)
head(sim$sequences)
sim$population

}
