% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04-delay.R
\name{surv_nowcast_lineage}
\alias{surv_nowcast_lineage}
\title{Nowcast lineage counts correcting for reporting delays}
\usage{
surv_nowcast_lineage(
  design,
  delay_fit,
  lineage = NULL,
  time = "epiweek",
  horizon = 4L,
  ref_date = NULL,
  method = c("direct", "em")
)
}
\arguments{
\item{design}{A \code{surv_design} object.}

\item{delay_fit}{A \code{surv_delay_fit} object.}

\item{lineage}{Character or \code{NULL}. Target lineage.}

\item{time}{Character. Default \code{"epiweek"}.}

\item{horizon}{Integer. Recent periods to nowcast. Default 4.}

\item{ref_date}{Date or \code{NULL}. Default max report date.}

\item{method}{Character: \code{"direct"} (default) or \code{"em"}.}
}
\value{
A \code{surv_nowcast} object.
}
\description{
Nowcast lineage counts correcting for reporting delays
}
\examples{
sim <- surv_simulate(n_regions = 3, n_weeks = 12, seed = 1)
d <- surv_design(sim$sequences, ~ region,
                 sim$population[c("region", "seq_rate")], sim$population)
fit <- surv_estimate_delay(d)
nc <- surv_nowcast_lineage(d, fit, "BA.2.86")
print(nc)

}
