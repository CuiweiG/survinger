% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/02-allocation.R
\name{surv_optimize_allocation}
\alias{surv_optimize_allocation}
\title{Optimize sequencing allocation across strata}
\usage{
surv_optimize_allocation(
  design,
  objective = c("min_mse", "max_detection", "min_imbalance"),
  total_capacity,
  budget = NULL,
  min_per_stratum = 2L,
  target_lineage = NULL,
  target_prevalence = 0.01,
  cost_col = NULL
)
}
\arguments{
\item{design}{A \code{surv_design} object.}

\item{objective}{Character. One of \code{"min_mse"}, \code{"max_detection"},
or \code{"min_imbalance"}.}

\item{total_capacity}{Integer. Total sequences available.}

\item{budget}{Numeric or \code{NULL}. Optional budget constraint.}

\item{min_per_stratum}{Integer. Minimum per stratum. Default 2.}

\item{target_lineage}{Character. Required for \code{"max_detection"}.}

\item{target_prevalence}{Numeric. Assumed prevalence for detection.
Default 0.01.}

\item{cost_col}{Character or \code{NULL}. Column name for per-sequence cost.}
}
\value{
A \code{surv_allocation} object.
}
\description{
Given fixed total sequencing capacity, finds the optimal allocation
across strata that minimizes a specified objective function.
}
\examples{
sim <- surv_simulate(n_regions = 4, n_weeks = 10, seed = 1)
d <- surv_design(sim$sequences, ~ region,
                 sim$population[c("region", "seq_rate")], sim$population)
a <- surv_optimize_allocation(d, "min_mse", total_capacity = 500)
print(a)

}
