% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12-advanced.R
\name{surv_power_curve}
\alias{surv_power_curve}
\alias{plot.surv_power_curve}
\title{Compute power curve for detection across prevalence range}
\usage{
surv_power_curve(
  design,
  prevalence_range = seq(0.001, 0.05, by = 0.001),
  delay_fit = NULL,
  thresholds = c(0.5, 0.8, 0.95)
)

\method{plot}{surv_power_curve}(x, ...)
}
\arguments{
\item{design}{A \code{surv_design} object.}

\item{prevalence_range}{Numeric vector of prevalences to evaluate.
Default \code{seq(0.001, 0.05, by = 0.001)}.}

\item{delay_fit}{Optional \code{surv_delay_fit}.}

\item{thresholds}{Numeric vector of detection thresholds to mark.
Default \code{c(0.5, 0.8, 0.95)}.}

\item{x}{A \code{surv_power_curve} object.}

\item{...}{Additional arguments (unused).}
}
\value{
A list with:
\describe{
\item{curve}{Tibble with prevalence and detection columns.}
\item{thresholds}{Tibble with threshold, prevalence_needed columns.}
}

A ggplot2 object.
}
\description{
Generates a detection probability curve that can be directly
plotted or included in publications. Answers: "At what prevalence
does our surveillance achieve X\% detection?"
}
\examples{
sim <- surv_simulate(n_regions = 3, n_weeks = 10, seed = 1)
d <- surv_design(sim$sequences, ~ region,
                 sim$population[c("region", "seq_rate")], sim$population)
pc <- surv_power_curve(d)
pc$thresholds

}
\seealso{
\code{\link[=surv_detection_probability]{surv_detection_probability()}}, \code{\link[=surv_required_sequences]{surv_required_sequences()}}
}
