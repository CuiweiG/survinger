% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/05-adjusted.R
\name{surv_adjusted_prevalence}
\alias{surv_adjusted_prevalence}
\title{Combined design-weighted and delay-adjusted prevalence}
\usage{
surv_adjusted_prevalence(
  design,
  delay_fit,
  lineage,
  time = "epiweek",
  prevalence_method = "hajek",
  nowcast_method = "direct",
  conf_level = 0.95,
  bootstrap_n = 0L
)
}
\arguments{
\item{design}{A \code{surv_design} object.}

\item{delay_fit}{A \code{surv_delay_fit} object.}

\item{lineage}{Character. Target lineage.}

\item{time}{Character. Default \code{"epiweek"}.}

\item{prevalence_method}{Character. Default \code{"hajek"}.}

\item{nowcast_method}{Character. Default \code{"direct"}.}

\item{conf_level}{Numeric. Default 0.95.}

\item{bootstrap_n}{Integer. 0 for delta method, >0 for bootstrap.
Default 0.}
}
\value{
A \code{surv_adjusted} object.
}
\description{
Simultaneously corrects for unequal sequencing rates and
right-truncation from reporting delays.
}
\examples{
sim <- surv_simulate(n_regions = 3, n_weeks = 12, seed = 1)
d <- surv_design(sim$sequences, ~ region,
                 sim$population[c("region", "seq_rate")], sim$population)
delay <- surv_estimate_delay(d)
adj <- surv_adjusted_prevalence(d, delay, "BA.2.86")
print(adj)

}
