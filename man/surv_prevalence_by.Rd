% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12-advanced.R
\name{surv_prevalence_by}
\alias{surv_prevalence_by}
\title{Estimate prevalence by subgroup}
\usage{
surv_prevalence_by(
  design,
  lineage,
  by,
  time = "epiweek",
  method = "hajek",
  conf_level = 0.95
)
}
\arguments{
\item{design}{A \code{surv_design} object.}

\item{lineage}{Character. Target lineage.}

\item{by}{Character. Column name to group by (e.g., \code{"region"} or
\code{"source_type"}).}

\item{time}{Character. Time aggregation. Default \code{"epiweek"}.}

\item{method}{Character. Estimation method. Default \code{"hajek"}.}

\item{conf_level}{Numeric. Default 0.95.}
}
\value{
A tibble with columns: group, time, lineage, prevalence, se,
ci_lower, ci_upper, n_obs, effective_n.
}
\description{
Applies \code{\link[=surv_lineage_prevalence]{surv_lineage_prevalence()}} within subgroups defined by
a grouping variable. Analogous to \code{\link[survey:svyby]{survey::svyby()}} for stratified
survey analysis.
}
\examples{
sim <- surv_simulate(n_regions = 4, n_weeks = 10, seed = 1)
d <- surv_design(sim$sequences, ~ region,
                 sim$population[c("region", "seq_rate")],
                 sim$population, source_type = "source_type")
surv_prevalence_by(d, "BA.2.86", by = "region")

}
\seealso{
\code{\link[=surv_lineage_prevalence]{surv_lineage_prevalence()}}, \code{\link[=surv_filter]{surv_filter()}}
}
