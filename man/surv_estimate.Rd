% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13-infrastructure.R
\name{surv_estimate}
\alias{surv_estimate}
\title{Pipe-friendly surveillance analysis}
\usage{
surv_estimate(
  data,
  strata,
  sequencing_rate,
  population,
  lineage,
  correct_delay = FALSE,
  method = "hajek",
  ...
)
}
\arguments{
\item{data}{Data frame of sequence records.}

\item{strata}{One-sided formula for stratification.}

\item{sequencing_rate}{Sequencing rate specification (see \code{\link[=surv_design]{surv_design()}}).}

\item{population}{Population data frame.}

\item{lineage}{Character. Target lineage to estimate.}

\item{correct_delay}{Logical. Apply delay correction? Default \code{FALSE}.}

\item{method}{Character. Prevalence method. Default \code{"hajek"}.}

\item{...}{Additional arguments passed to \code{\link[=surv_design]{surv_design()}}.}
}
\value{
A \code{surv_prevalence} or \code{surv_adjusted} object.
}
\description{
Convenience wrapper that creates a design, estimates prevalence,
and optionally applies delay correction in a single pipe-friendly call.
Designed for rapid exploratory analysis in interactive sessions.
}
\examples{
sim <- surv_simulate(n_regions = 3, n_weeks = 10, seed = 1)
# One-liner analysis:
result <- surv_estimate(
  data = sim$sequences, strata = ~ region,
  sequencing_rate = sim$population[c("region", "seq_rate")],
  population = sim$population,
  lineage = "BA.2.86"
)
print(result)

}
\seealso{
\code{\link[=surv_design]{surv_design()}}, \code{\link[=surv_lineage_prevalence]{surv_lineage_prevalence()}}
}
