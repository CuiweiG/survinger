% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13-infrastructure.R
\name{surv_table}
\alias{surv_table}
\title{Format prevalence results for knitr tables}
\usage{
surv_table(x, digits = 3, percent = TRUE, ...)
}
\arguments{
\item{x}{A survinger result object.}

\item{digits}{Integer. Decimal places for prevalence. Default 3.}

\item{percent}{Logical. Display as percentages? Default \code{TRUE}.}

\item{...}{Additional arguments (unused).}
}
\value{
A tibble formatted for display.
}
\description{
Produces a publication-ready summary table from any survinger result.
Automatically called when objects are printed inside RMarkdown chunks
if \code{knitr} is loaded.
}
\examples{
sim <- surv_simulate(n_regions = 3, n_weeks = 10, seed = 1)
d <- surv_design(sim$sequences, ~ region,
                 sim$population[c("region", "seq_rate")], sim$population)
prev <- surv_lineage_prevalence(d, "BA.2.86")
surv_table(prev)

}
\seealso{
\code{\link[=surv_lineage_prevalence]{surv_lineage_prevalence()}}, \code{\link[=tidy.surv_prevalence]{tidy.surv_prevalence()}}
}
